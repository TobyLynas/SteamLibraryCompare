import styles from "../styles/hostSetup.module.css";

const hostSetup = () => {
    return(
        <div className={styles.main}>
            Hello World!
        </div>
    );
};

export default hostSetup;