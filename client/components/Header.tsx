import React from "react";
import styles from "../styles/header.module.css";

const Header = () => (
    <div className={styles.header}>
        <p className={styles.heading}>Steam Library</p>
    </div>
);

export default Header;
